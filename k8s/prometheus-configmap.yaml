apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: crypto-streaming

data:
  prometheus.yml: |
    global:
      scrape_interval: 5s

    scrape_configs:
      - job_name: "crypto-api"
        metrics_path: /metrics
        static_configs:
          - targets: ["crypto-api-service:80"]